spring:
  application:
    name: agenor-spring-boot-example

server:
  port: 8080

agenor:
  runtime:
    name: example-runtime
    environment: development
  agents:
    base-package: dev.agenor.example.springboot.agent
  # To enable LLM support, uncomment both this block AND the agenor-adapters
  # dependency in pom.xml. Then POST /api/llm-greet to trigger LlmGreeterAgent.
  # llm:
  #   provider: openai
  #   api-key: ${OPENAI_API_KEY}
  #   model: gpt-4o-mini

management:
  endpoints:
    web:
      exposure:
        include: health, info
  endpoint:
    health:
      show-details: always